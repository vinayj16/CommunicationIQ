"""Tier 0 — heuristic implementations.

Deliberately empty at M0. The contracts and the registry that will select
these arrive first, in keeping with the BRD's rule that no capability is built
before its contract. The implementations land with M1, when there is an
attempt to score.
"""
