# The Tier-1 speech engine. Install alongside requirements.txt.
#
# Needs roughly 2.5-3.5 GB of RAM resident while scoring, which is why it is
# separate: a 512 MB instance cannot hold it, and being OOM-killed mid-request
# reaches a student as a lost attempt.

faster-whisper==1.1.1
onnxruntime==1.28.0

# CPU wheels, explicitly. Default PyPI torch pulls the CUDA build: ~2.5 GB of
# GPU runtime that will never execute on a CPU instance.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.13.0
torchaudio==2.11.0
transformers==5.15.0
